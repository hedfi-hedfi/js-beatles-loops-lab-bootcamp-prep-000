// add solution here
function theBeatlesPlay(arrayMusicians, arrayInstruments){
  let variable = [];
  for(let i = 0; i < arrayMusicians; i++){
    variable.push(arrayMusicians[i] + ' plays ' + arrayInstruments[i]);
  }
  return variable;
}

function johnLennonFacts(array) {
  let n = array.length - 1;
  while(i >= 0) {
    array[i] += '!!!';
    n--;
  }
  return '!!!';
}